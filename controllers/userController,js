// Sample function to handle user actions
const getUser = (req, res) => {
    // Logic for getting user information (This is just an example)
    res.send("User info goes here");
};

  // Export the function so it can be used in routes
module.exports = {
    getUser,
};