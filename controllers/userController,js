// controllers/userController.js

// Sample function to handle user actions
const getUser = (req, res) => {
    // Logic for getting user information
    res.send("User info goes here");
};

module.exports = {
    getUser, // Export the getUser function to be used in routes
};